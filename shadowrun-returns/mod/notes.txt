-- 11/03/2026 --
I'm up to building the python pipeline to NLP the .po text

This is because we understand, roughly, how the text mouse
interaction will work. That is, there is box that the text
is drawn in, and within that box, each text element has a
set of vertices. Check if the mouse is in between those
vertices, and you're good

We've gone with the idea of delineating words in a language
like chinese with invisible spaces. Its very elegant and
allows us to embed string segmentation into the text itself,
so that we don't have to worry for a language like chinese.

Then, with string segmentation built in, we can, as we
loop through the words and build their mappings from 
'text on screen' to 'meta data object', easily say
'this word X is word 3 in this sentence, and meta data
objects word 3 is this'

This allows us to minimally edit the text (you know,
minus the adding of pinyin and so on), whilst allowing
the NLP to essentially give us spaces.


--- 
Sorry, why are we using the invisible characters again?

We have a list of vertices, 4 to a character...

we'd have a list of words in the sentence

we'd have a mapping number: word

我不知道 becomes {0: 我, 1: 不, 2: 知道, 3: 知道}

this does make it hard because now you need to find the substring?

If you need to find the text...

Then you can use the map itself again:

land on character 4

note that its two characters, what are the preceeding and proceeding items in
the map

if they are a match, you're golden. However this does eliminate usage when
a word is just repeated? you would have to specifically look for the first
character 知

if you had invisible characters though, you land on 3:

look left for invisible character, look right for invisible character

collect from left to right, and load